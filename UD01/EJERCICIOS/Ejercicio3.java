public class Ejercicio3 {
    public static void main (String [] args) {
        int n=1; // Funciona, pero no es recomendable ya que se recomienda que se use el estilo lowCamelCase, y que sean preferiblemente palabras descriptivas.
        int MiProblema=2; // Funciona, pero no es recomendable ya que se recomienda que se use el estilo lowCamelCase, y que sean preferiblemente palabras descriptivas.
        int MiJuego=3; // Funciona, pero no es recomendable ya que se recomienda que se use el estilo lowCamelCase, y que sean preferiblemente palabras descriptivas.
        int Mi Juego=4; // No funciona, porque hay un espacio y da error.
        int Int=5; // No funciona, tiene una palabra clave como nombre de variable.
        int Jose&Co=6; // No funciona, tiene un caracter especial, "&" se usa como un operador lógico.
        int A b=7; // No funciona, porque hay un espacio y da error.
        int 1rApellido=8; // No funciona, empieza con un número y los nombres de variables solo pueden empezar con un carácter. 
        int aaaaaaaaaaaa=9; // Funciona, pero no es recomendable ya que se recomienda que se use el estilo lowCamelCase, y que sean preferiblemente palabras descriptivas.
        int Nombre_Apellidos=10; // Funciona, pero no es recomendable ya que se recomienda que se use el estilo lowCamelCase, y que sean preferiblemente palabras descriptivas.
        int Saldo-actual=11; // No funciona, interpreta el símbolo "-" como una resta entre el nombre de variable.
        int Universidad Alicante=12 // No funciona, porque hay un espacio y da error.
        int Juan=Rubio=13 // No funciona, interpreta el símbolo "=" como una asignación de valor entre el nombre de variable.
        int Edad5=14 // Funciona, pero no es recomendable ya que se recomienda que se use el estilo lowCamelCase, y que sean preferiblemente palabras descriptivas.
        int _5Java=15 // Funciona, pero no es recomendable ya que se recomienda que se use el estilo lowCamelCase, y que sean preferiblemente palabras descriptivas.
        
        System.out.println();
    }
}