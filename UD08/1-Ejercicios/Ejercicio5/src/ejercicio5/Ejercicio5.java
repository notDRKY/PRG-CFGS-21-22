/*
 * 5. De cada pareja de afirmaciones indica cual es la verdaderas:
 *
 * 1.
 * Se dice que instanciamos una clase cuando creamos objetos de dicha
 * clase. TRUE
 * Se dice que instanciamos una clase cuando creamos una subclase de
 * dicha clase. FALSE
 * 2.
 * Si una clase es abstracta no se puede instanciar. TRUE
 * Si una clase es abstracta no se puede heredar de ella. FALSE
 * 3.
 * Una clase abstracta tiene que tener métodos abstractos. FALSE
 * Una clase puede ser abstracta y no tener métodos abstractos. TRUE
 * 4.
 * Si una clase tiene métodos abstractos tiene que ser abstracta. TRUE
 * Una clase puede tener métodos abstractos y no ser abstracta. FALSE
 * 5.
 * Si una clase es abstracta sus subclases no pueden ser abstractas. FALSE
 * Una clase abstracta puede tener subclases que también sean
 * abstractas. TRUE
 * 6.
 * Si un método es abstracto en una clase, tiene que ser no abstracto en
 * la subclase, o bien, la subclase tiene que ser también abstracta. TRUE
 * Si un método es abstracto en una clase, no puede ser abstracto en las
 * subclases. FALSE
 * 7.
 * Si un método se define final se tiene que reescribir en las subclases. FALSE
 * Si un método se define final no se puede reescribir en las subclases. TRUE
 * 8.
 * Una clase puede tener un método final y no ser una clase final. TRUE
 * Si una clase tiene un método final tiene que ser una clase final. FALSE
 * 9.
 * Si una clase se define final no se pueden definir subclases de ella. TRUE
 * Si una clase se define final no se puede instanciar. FALSE
 * 10.
 * Un método definido final y abstract resultaría inútil, puesto que nunca
 * se podría implementar en las subclases. TRUE
 * Un método definido final y abstract podría resultar útil. FALSE
 * 11.
 * Una clase definida final y abstract resultaría inútil, puesto que no se
 * podría instanciar ni heredar de ella. TRUE
 * Una clase definida final y abstract podría resultar útil. FALSE
 *
 */
package ejercicio5;

public class Ejercicio5 {

    public static void main(String[] args) {

    }

}
